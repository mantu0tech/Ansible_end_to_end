Now I’ll give you a step-by-step working setup + scripts to create:

EC2 instance
S3 bucket
Security Group

ansible-galaxy collection install amazon.aws
pip install boto3 botocore

aws cli should be there 
sudo apt install awscli
